@using Nat.Web.Core.Kendo.Extensions
@model DateTime?

@(Html.Kendo().DatePickerFor(m => m)
      .DefaultParseFormats()
      .Events(e => e.Change("Nat.F.DateAlert"))
      .Deferred((bool?) ViewBag.EditorTemplateDeferred ?? false))